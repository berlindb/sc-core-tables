name: Readiness

# Recomputes Sugar Calendar's behavioral (column-flag) readiness against berlindb/core
# master and opens a PR if the badge changed. SC's flags live in its own fork schemas,
# so this installs SC (like the regenerate job) and scores those classes.

on:
  schedule:
    - cron: '37 6 * * *'   # daily
  workflow_dispatch:

jobs:
  readiness:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: wordpress
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout plugin
        uses: actions/checkout@v6

      - name: Checkout berlindb/core
        uses: actions/checkout@v6
        with:
          repository: berlindb/core
          ref: master
          path: berlindb-core

      - name: Setup PHP + WP-CLI
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          extensions: mysqli
          tools: composer, wp-cli
          coverage: none

      - name: Install dependencies
        run: |
          composer config repositories.berlindb-core path ./berlindb-core
          composer update --no-interaction --prefer-dist --no-progress

      - name: Use mysql_native_password auth
        run: mysql -h 127.0.0.1 -P 3306 -u root -proot --ssl-mode=DISABLED --get-server-public-key -e "ALTER USER 'root'@'%' IDENTIFIED WITH mysql_native_password BY 'root';"

      - name: Set up a throwaway WordPress
        run: |
          wp core download --path=wp --version=latest
          wp config create --path=wp --dbname=wordpress --dbuser=root --dbpass=root --dbhost=127.0.0.1:3306
          wp core install --path=wp --url=http://localhost --title=scct --admin_user=admin --admin_password=admin --admin_email=admin@example.com --skip-email

      - name: Install + activate latest Sugar Calendar (Lite)
        run: |
          wp --path=wp plugin install sugar-calendar-lite --activate
          echo "SC_VERSION=$(wp --path=wp plugin get sugar-calendar-lite --field=version)" >> "$GITHUB_ENV"

      - name: Score readiness from the live SC fork
        run: wp --path=wp eval-file bin/readiness-report.php

      - name: Open a PR if the badge changed
        uses: peter-evans/create-pull-request@v7
        with:
          branch: readiness/sugar-calendar
          title: "Update Sugar Calendar readiness badge"
          commit-message: "Update Sugar Calendar readiness badge (SC ${{ env.SC_VERSION }} vs core master)"
          body: |
            Automated behavioral-readiness recomputation (Sugar Calendar
            **${{ env.SC_VERSION }}** fork flags vs berlindb/core master). If the badge
            changed, a flag went supported <-> gap - review before merging.
          add-paths: .readiness/
          delete-branch: true
